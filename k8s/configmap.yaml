apiVersion: v1
kind: ConfigMap
metadata:
  name: job-scheduler-config
  namespace: job-scheduler
data:
  PORT: "8080"
  REDIS_ADDR: "redis-service:6379"
  DATABASE_URL: "postgres://postgres:postgres@postgres-service:5432/jobscheduler?sslmode=disable"
  WORKER_CONCURRENCY: "5"
